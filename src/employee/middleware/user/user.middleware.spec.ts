// import { EmployeeMiddleware } from './employee.middleware';
import { EmployeeMiddleware } from './employee.middleware';

describe('EmployeeMiddleware', () => {
  it('should be defined', () => {
    // expect(new EmployeeMiddleware()).toBeDefined();
  });
});
